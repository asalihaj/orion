export interface IContact {
    id: string;
    username: any;
    title: string;
    description: string;
    dateCreated: string;
}

export interface IContactFormValues {
    id: string;
    userId: string;
    email: string;
    title: string;
    description: string;
}