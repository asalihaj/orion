export interface IResume {
    offerId: string;
    jobSeekerId: string;
    cv: string;
    lastUpdated: Date;
}

export class IResumeFormValues {
    offerId: string;
    jobSeekerId: string;
    cv: string;
}